service pop3s
{
        socket_type             = stream
        protocol                = tcp
        wait                    = no
        user                    = root
        server                  = /usr/local/sbin/popper
        server_args             = qpopper -f /etc/qpopper/config-tls
        disable                 = no
}

